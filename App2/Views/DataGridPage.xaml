<Page
    x:Class="App2.Views.DataGridPage"
    DataContext="{Binding DataGridViewModel, Source={StaticResource Locator}}"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:properties="clr-namespace:App2.Properties"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800"
    >
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Style="{StaticResource PageTitleStyle}"
            Margin="{StaticResource MediumLeftMargin}"
            Text="{x:Static properties:Resources.DataGridPageTitle}" />

        <Grid Grid.Row="1">
            <DataGrid
                AutoGenerateColumns="True"
                GridLinesVisibility="All"
                CanUserAddRows="False"
                ItemsSource="{Binding Source}"
                Margin="{StaticResource SmallLeftRightMargin}"
                KeyboardNavigation.TabNavigation="Once" HorizontalAlignment="Left" VerticalAlignment="Top" BorderThickness="1,1,1,1">
             </DataGrid>
        </Grid>
    </Grid>
</Page>